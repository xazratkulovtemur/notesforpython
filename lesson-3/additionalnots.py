#to find length of sets, we just can use len( ) method

#to check subset, we should write sublist like frozenset({1,2})

# we can remove element from set, by remove() method. 
#numbers.remove(a)
#print(numbers)

# to add elements to SET, we use add() method

#to add dicts we use update() method