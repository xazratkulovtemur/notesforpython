#int
#float
#complex
import math
a=100_000_000 # _ is ignored, just to distinguish integrar we use.
b=1e10 # it means 1*10^10 
pow (a,b) #instead of **, we can use pow. it is built in function
import math #built in Library
math.sqrt() #square root
f=5
d=3
print(pow(f,d))
#In complex numbers, there is real(real) and imaginary(imag) parts
c=3+5j
c.real=3 , c.imag=5

